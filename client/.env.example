# Mismo Client ID que GOOGLE_CLIENT_ID en server/.env
VITE_GOOGLE_CLIENT_ID="tu-client-id.apps.googleusercontent.com"

# --- Vercel (paso 4) ---
# Añade VITE_GOOGLE_CLIENT_ID en el dashboard de Vercel (Environment Variables).
# En Google Cloud Console → OAuth → Authorized JavaScript origins:
#   https://tu-proyecto.vercel.app
# Si la URL de Render no es myworkout-api.onrender.com, edita vercel.json (rewrites).
